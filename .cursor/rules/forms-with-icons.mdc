---
description: Formularios Blade siempre con iconos Tabler (input-icon + botones)
globs: resources/views/**/*.blade.php
alwaysApply: false
---

# Formularios con iconos

SIEMPRE usa iconos Tabler Icons (`ti ti-*`) en formularios.

## HTTP admin

Solo usa las funciones centralizadas:

```js
BanHttp.get('/users/1/edit')
BanHttp.post('/users', payload)
```

Archivo: `resources/js/admin.js`

## Inputs

Usa el componente `x-form.input` o el patrón Tabler `input-icon`:

```blade
<div class="mb-3">
    <label class="form-label" for="email">Correo electrónico</label>
    <div class="input-icon">
        <span class="input-icon-addon">
            <i class="ti ti-mail"></i>
        </span>
        <input type="email" name="email" id="email" class="form-control">
    </div>
</div>
```

## Botones de formulario

Incluye icono a la izquierda del texto:

```blade
<button type="submit" class="btn btn-primary">
    <i class="ti ti-device-floppy me-1"></i>
    Guardar
</button>
```
